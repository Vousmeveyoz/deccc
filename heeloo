– This function creates a TextButton that explodes the player when clicked.
– @param player: The player who clicked the button.
local function explodePlayer(player)
    – Check if the player is valid and alive.
    if player and player.Character and player.Character:FindFirstChild(“Humanoid”) and player.Character.Humanoid.Health > 0 then
        – Create an explosion at the player’s position.
        local explosion = Instance.new(“Explosion”)
        explosion.Position = player.Character.HumanoidRootPart.Position
        explosion.Parent = workspace
    -- Set the player's health to zero to simulate an explosion.
    player.Character.Humanoid.Health = 0
end

end
– Create a ScreenGui to hold the TextButton.
local screenGui = Instance.new(“ScreenGui”)
screenGui.Parent = game:GetService(“StarterGui”)
– Create a TextButton inside the ScreenGui.
local textButton = Instance.new(“TextButton”)
textButton.Text = “Explode”
textButton.Size = UDim2.new(0, 200, 0, 50)
textButton.Position = UDim2.new(0.5, -100, 0.5, -25)
textButton.Parent = screenGui
– Connect the explodePlayer function to the TextButton’s MouseButton1Click event.
textButton.MouseButton1Click:Connect(function()
    – Get the player who clicked the button.
    local player = game.Players.LocalPlayer
    explodePlayer(player)
end)
